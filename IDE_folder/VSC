Visual Studio Studio 
https://www.youtube.com/watch?v=v2_Pth8MrKA
#1 For CPP
Step 1: Go to @extention 
install Code Runner
  #for C, C++, Java,python, need to install "code runner"   
    -search "code runner" and click the one with .run and install
install C/C++ intellsense, debugging, code browser

"""""""""
Finish above step, you click "run code" for c++ file and display output at "Output"  
"""""""""

Step 3: installing the Clang compiler for macbook (default should be installed already)
@terminal     (for macbook pro, better use clang)
xcode-select --install
clang --version

option for other compliers: gcc, clang, and Apple's LLVM
gcc:
brew install gcc
clang
brew install llvm

""""""""
when finish above step, you can input similar command to compile 
clang++ filename.cpp -o filename     #it'll generate compile file 
./filename at terminal 
""""""""

STep 2: Setting -> type"run code" ->click "run code configuation" at left-side bar-> then check below items
code runner: enable app insights
code runner: run in terminal
code runner: save file before run

Step 4: 
https://stackoverflow.com/questions/27924050/expected-at-end-of-declaration-vector-c
search "executor map"
Under "Code-runner: Executor Map" -> click on "Edit in setting.json"
in "code-runner.executorMap"

Solution: add -std=c++14            #c++20, c++17
"cpp": "cd $dir && g++ -std=c++14 $fileName -o $fileNameWithoutExt && $dir$fileNameWithoutExt"


#2 for Python3


#2 For Python
